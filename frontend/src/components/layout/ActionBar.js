import styles from "./ActionBar.module.css";

const ActionBar = () => {
  return (
    <div className={styles.actionBarLayout}>
      <h2>Action Bar</h2>
    </div>
  );
};

export default ActionBar;
